/**
 * @file: profile.config.ts
 * @author: Sourav Giri
 * @summary: Full-Stack Developer | AI/LLM Enthusiast | Open to Opportunities
 */

// --------------------------------------------------
// 1. CORE DEPENDENCIES & SKILLS
// --------------------------------------------------

const TECH_STACK = {
  frontend: [
    "React.js",      // 90%
    "Next.js",       // 85%
    "TypeScript",    // 85%
    "TailwindCSS",   // 90%
    "Framer Motion", // 80%
    "Three.js",      // 70%
  ],
  backend: [
    "Node.js",       // 85%
    "Express.js",    // 85%
    "FastAPI",       // 75%
    "Socket.io",     // 80%
    "MongoDB",       // 80%
    "PostgreSQL",    // 80%
    "Prisma ORM",    // 75%
    "Redis",         // 65%
  ],
  ai_ml: [
    "LangChain",     // 80%
    "OpenAI API",    // 85%
    "Agentic AI",    // 75%
    "RAG Pipelines", // 70%
    "Prompt Engg.",  // 85%
    "Vector DBs",    // 65%
  ],
  devops: [
    "Docker",        // 75%
    "AWS (EC2/S3)",  // 70%
    "Git/GitHub",    // 90%
    "Linux",         // 75%
  ]
};

// --------------------------------------------------
// 2. PROJECT LOG (Reverse Chronological)
// --------------------------------------------------

class ProjectHistory {

  /**
   * FLAGSHIP PROJECT
   * @period 2026
   */
  public async KiwiOS(): Promise<Impact> {
    const type = "Personal Portfolio";

    const stack = ["React", "Vite", "Framer Motion", "SCSS"];
    const highlight = "macOS-inspired desktop UI with draggable windows, CLI terminal, Spotify embed";
    const status = "Live";

    return { type, stack, highlight, status };
  }

  /**
   * AI PROJECT
   * @period 2026
   */
  public async Moodify(): Promise<Impact> {
    const type = "AI-Powered App";

    const stack = ["React", "Node.js", "google mediapipe model"];
    const highlight = "Real-time mood detection → personalized music recommendations via LLM pipeline";
    const status = "Live";

    return { type, stack, highlight, status };
  }

  /**
   * REAL-TIME PROJECT
   * @period 2026
   */
  public RealTimeCodeEditor(): void {
    this.type = "Collaborative Tool";
    this.stack = ["React", "Node.js", "Socket.io", "Monaco Editor"];

    // Multi-user live code execution with room-based chat
    // Pair programming sessions with <100ms sync latency
  }

  /**
   * PRODUCTIVITY APP
   * @period 2026
   */
  public CalendarMarker(): void {
    this.type = "Productivity App";
    this.stack = ["react", "mogngodb", "jwt", "TailwindCSS","Node js];

    // Smart scheduling with priority tagging + deadline reminders
    // Deployed on AWS with Docker containerization
  }
}

// --------------------------------------------------
// 3. WHAT I BRING TO THE TABLE
// --------------------------------------------------

interface FresherEdge {
  superpower: string;
  proof: string;
}

const myEdge: FresherEdge[] = [
  {
    superpower: "AI-First Thinking",
    proof: "Integrate LLMs into real products, not just toy projects",
  },
  {
    superpower: "Full Stack Ownership",
    proof: "Ship features end-to-end — UI to DB to deployment",
  },
  {
    superpower: "Fast Learner",
    proof: "Self-taught Docker, AWS, LangChain while in college",
  },
  {
    superpower: "Real World Code",
    proof: "All projects live, containerized, and cloud-deployed",
  }
];

// --------------------------------------------------
// 4. ACADEMIC BACKGROUND
// --------------------------------------------------

const education = new Map<string, string>([
  ["HIT - Haldia Institute of Technology", "B.Tech"],
]);

// --------------------------------------------------
// 5. CURRENTLY EXPLORING
// --------------------------------------------------

const learning: string[] = [
  "Multi-Agent AI Systems",   // AutoGen, CrewAI
  "Next.js 15 App Router",
  "tRPC + Turborepo",
  "System Design Fundamentals",
];

export default new ProjectHistory();